# Your Ethereum node provider URL (e.g., from Alchemy or Infura)
PROVIDER_URL=https://mainnet.infura.io/v3/YOUR_API_KEY
WSS_PROVIDER_URL=wss://mainnet.infura.io/ws/v3/YOUR_API_KEY

# The private key of the wallet that will SIGN transactions (the "Searcher")
# This wallet ONLY needs enough ETH to pay for failed bundle fees (if any), not the main capital.
SEARCHER_PRIVATE_KEY=0x...

# The private key of the wallet to receive profits (can be the same or different)
BOT_OWNER_ADDRESS=0x...
